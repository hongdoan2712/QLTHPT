@model IEnumerable<QLTHPT.Models.THONGTINDAOTAO>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CANBO.CB_HOTEN)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HINHTHUC.HT_TEN)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CANBO.CB_HOTEN)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HINHTHUC.HT_TEN)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TTDT_MA }) |
            @Html.ActionLink("Details", "Details", new { id=item.TTDT_MA }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TTDT_MA })
        </td>
    </tr>
}

</table>
