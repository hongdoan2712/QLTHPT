@model IEnumerable<QLTHPT.Models.THONGTINLUONG>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TTL_NGAYNHANLUONG)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TTL_HESOLUONG)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BACLUONG.BL_TEN)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NGACHLUONG.NL_TEN)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TTL_NGAYNHANLUONG)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TTL_HESOLUONG)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BACLUONG.BL_TEN)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NGACHLUONG.NL_TEN)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TTL_MA }) |
            @Html.ActionLink("Details", "Details", new { id=item.TTL_MA }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TTL_MA })
        </td>
    </tr>
}

</table>
