@model IEnumerable<QLTHPT.Models.CHITIET_CHUCVU>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CT_CV_TEN)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CANBO.CB_HOTEN)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CHUCVU.CV_TEN)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CT_CV_TEN)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CANBO.CB_HOTEN)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CHUCVU.CV_TEN)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CT_CV_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CT_CV_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CT_CV_ID })
        </td>
    </tr>
}

</table>
