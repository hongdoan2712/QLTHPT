@model IEnumerable<QLTHPT.Models.SODANHGIA>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SDG_DIEM)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SDG_GHICHU)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NAMHOC.NH_NAMHOC)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SDG_DIEM)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SDG_GHICHU)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NAMHOC.NH_NAMHOC)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SDG_MA }) |
            @Html.ActionLink("Details", "Details", new { id=item.SDG_MA }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SDG_MA })
        </td>
    </tr>
}

</table>
